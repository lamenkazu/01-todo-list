import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  empty: {
    marginTop: 48,
    alignItems: "center",
  },

  clipboardImage: {
    marginBottom: 16,
  },

  textStrong: {
    color: "#808080",
    fontWeight: "bold",
    fontSize: 18,
  },
  textCommon: {
    color: "#808080",
    fontSize: 18,
  },
});
